# 🚀 마인크래프트 AI 백엔드 의존성
# Gemini 2.5 Pro 웹검색 기반 간소화 아키텍처

# 핵심 웹 서버
Flask==2.3.3
Flask-CORS==4.0.0

# AI 모델 SDK
google-genai==0.3.0          # Gemini 2.5 Pro (메인, 웹검색 지원)
openai==1.3.0                # GPT (백업, 선택사항)
anthropic==0.7.0             # Claude (백업, 선택사항)

# 유틸리티
requests==2.31.0             # HTTP 요청
python-dotenv==1.0.0         # 환경변수 관리

# 보안 및 모니터링
bleach==6.1.0                # 입력 데이터 정제
psutil==5.9.6                # 시스템 리소스 모니터링

# RAG (벡터 검색)
sentence-transformers==2.2.2
faiss-cpu==1.7.4
tqdm==4.66.4
numpy==1.26.4

# GCP RAG 시스템 (기본 활성화)
google-cloud-firestore==2.21.0
google-cloud-aiplatform==1.71.1
vertexai==1.71.1

# 테스트
pytest==7.4.3               # 단위 테스트
pytest-mock==3.12.0         # Mock 테스트